

function plotVectorAreas(av, st, vecStart, vecEnd)

figure; 

for v = 1:3
    axH(v) = subplot(3,5,(v-1)*5+1); 
end

plotBrainOutlinesByAxis(axH, av);

% convert vector from bregma to CCF

% plot vector on each projection

    axes(axH(v));
    
    hold on; 
    plot([vecStart(
        
% plot vector in 3D


% plot the list of areas the vector goes through